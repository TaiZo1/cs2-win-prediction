# CS2 Win Prediction — minimal dependencies for notebooks/02_eda.ipynb and
# notebooks/03_modeling.ipynb (assumes the CSVs in data/processed/ already exist).
#
# To re-run the parsing pipeline (parse .dem -> per-map CSVs), also install:
#     pip install "demoparser2>=0.40.0" "awpy>=2.0.0"
# To re-run the HLTV scraping, also install:
#     pip install "undetected-chromedriver>=3.5" "selenium>=4.15"

# ---- Core ----
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.11

# ---- Plots ----
matplotlib>=3.7
seaborn>=0.13

# ---- ML (PCA, logistic regression, scaling) ----
scikit-learn>=1.3

# ---- Jupyter ----
jupyter>=1.0
notebook>=7.0
ipykernel>=6.0
